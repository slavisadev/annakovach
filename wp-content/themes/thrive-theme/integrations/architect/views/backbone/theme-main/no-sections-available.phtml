<?php
/**
 * Thrive Themes - https://thrivethemes.com
 *
 * @package thrive-theme
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit; // Silence is golden!
}

?>

<div class="tth-no-templates">
	<?php Thrive_Views::svg_icon( 'no-sections' ); ?>
	<p>
		<?php echo __( "You didn't save any ", THEME_DOMAIN ); ?>
		<#= modal.section.type #>
		<?php echo __( ' sections for the current theme.', THEME_DOMAIN ); ?>
	</p>
	<p>
		<?php echo __( 'Go to ', THEME_DOMAIN ); ?>
		<span class="blue-text click" data-fn="renderFirstTab">
			<?php echo __( 'Theme', THEME_DOMAIN ); ?>
				<#= modal.displayName #>
			<?php echo __( ' Sections,', THEME_DOMAIN ); ?>
		</span>
		<?php echo __( 'choose a template, start editing it and when you finish press Save.', THEME_DOMAIN ); ?>
	</p>
</div>
